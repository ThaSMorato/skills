<!--
TEMPLATE: guidelines (the project's engineering ROUTER)
Filled by the guideline-generator, once per project. This document is an INDEX, not a manual: it says
where each standard lives and when to load it, so a stage that needs twenty lines about naming does
not pay for a thousand lines about everything else.
The depth lives in stack guides (.claude/skills/<tech>-guide/), generated by /generate-stack-guide.
HARD CAP: 150 lines. If it grows past that, the content belongs in a stack guide, not here.
PRUNE + RENUMBER optional sections that don't apply; (required) sections always stay.
-->

# Engineering Guidelines — <project>

> The router for this repo's standards. Read the tables, then load only the entry that covers what you
> are touching. Everything here is a pointer with a trigger; nothing here is a tutorial.

## 1. Project stack (required)
> What is actually used, at a glance. One row per technology, each pointing at the guide that owns its
> conventions. Mark each as specified by the user or auto-detected from the repo.

| Technology | Version | Source | Guide |
|---|---|---|---|
| <language> | <x.y> | specified \| detected | `.claude/skills/<tech>-guide/` |

## 2. Stack guide routing (required)
> Which guide applies to which files. A stage that touches `.rb` loads the Ruby guide and nothing else
> — this table is what makes that choice deterministic instead of a guess.

| When you touch | Load |
|---|---|
| `**/*.rb` | `ruby-guide` |
| `**/*.tsx`, `**/*.jsx` | `react-guide` |
| test files (`<pattern>`) | `testing-guide-<project>` |

> Missing guide? Generate it with `/generate-stack-guide <tech>`. Until it exists, the generic
> `clean-code`, `code-smells`, and `testing` skills are the fallback standard.

## 3. Non-negotiables (required)
> The handful of rules that hold everywhere in this repo, regardless of language. Keep this short —
> a long list of non-negotiables is a list of suggestions. Anything language-specific belongs in a
> stack guide.

## 4. Project structure (required)
> Where things live, as a table of directory → what belongs there. One line each; the reasoning belongs
> in the architecture docs, linked below.

| Path | Holds |
|---|---|

## 5. Commands (required)
> The real commands this repo runs, discovered from its own manifests — not the ecosystem's defaults.
> Plans reference these verbatim in their Deliverables.

| Purpose | Command |
|---|---|
| Test | |
| Type-check | |
| Lint | |
| Build | |
| Run locally | |

## 6. Project documents (required)
> The rest of the docs, and when each one is worth loading.

| Document | Load it when |
|---|---|
| `CONTEXT.md` | naming anything — it owns the vocabulary |
| `docs/adr/index.md` | a decision in the area is binding |
| `docs/components.md` | you need the component map |
| `docs/boundaries.md` | you are adding a dependency between components |
| `docs/hld.md` · `docs/fdd/*.md` | specifying or verifying behavior |

## 7. External references (optional)
> Authoritative external sources, with what each one answers. Links, not summaries.

| Source | Answers |
|---|---|

## 8. Pre-commit checklist (required)
> What must be true before a change lands, as checkable lines — each one either a command from section
> 5 or an objectively verifiable condition.
- [ ]
